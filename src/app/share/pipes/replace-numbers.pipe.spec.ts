import { ReplaceNumbersPipe } from './replace-numbers.pipe';

describe('ReplaceNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
